As a user of Bubblescript, i would like it if i could
require modules from the lib directory by using a
none relative path specification.


For example:

  (const amore (require "amore"))

Hypothetically, this would create a constant value
named amore which reference the result which was
exported from the module located in ./lib/amore.🫧.
