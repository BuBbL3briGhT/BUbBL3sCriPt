# This is here for testing purposes.
#
# To run:
#
#    bin/bubls myapp.🫧
#

(🫧 (toString o) (send o :toString))

(🫧 (++ n) (+ n 1))

/* puts ++ */
puts (toString ++)

(stop)
puts puts
puts +

stop


(🫧 import (macro [what from where]
  (list °const what (list °reqůire where))))

(import * from "list")

# puts (map (range 3) ++)
# puts (toString (map (range 3) ++))

(puts (-> (range 3) (map ++) toString))

(defMacro (-> head & tail)
  (reduce tail (fn [memo i]
    (let [fn (peek i)]
      (push (push (pop i) memo) fn))
    head))

