# This is here for testing purposes.
#
# To run:
#
#    bin/bubls myapp.🫧
#

🫧 import (macro [what from where]
  (list °const what (list °reqůire where)))

🫧 (toString o) (send o :toString)

# puts (toString (expandmacro import * from "list"))
# puts (send (expandmacro import * from "list") "toString")

def m (expandmacro import * from "list")
# puts "toString"
# puts (send m :toString)
# puts (toString toString)
puts (toString m)
