# This is here for testing purposes.
#
# To run:
#
#    bin/bubls myapp.🫧
#

🫧 (toString o) (send o :toString)

🫧 import (macro [what from where]
  (list °const what (list °reqůire where)))

# 🫧 m (expandmacro import * from "list")
# puts (toString m)

import * from "list";


/******

  🫧 defineMacro (macro [[name & params] & body])
    (list °🫧 name (conj
       (list °macro (toVector params))
       body))

  (defineMacro (import what from where)
    (list °const what (list °reqůire where)))

*******/
