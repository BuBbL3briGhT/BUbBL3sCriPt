September:
  - better stack trace by keeping a representation of
    the stack. Currently line and column point to
    definition of function, but the should point to
    the line of execution within the function

August:
✅️ Get tests to pass
- Actually we could just do
  - const [menu [sad happy]] xoxo This should
    work differently depending if xoxo is an
    object or a list. Assuming xoxo is an
    object, menu would be assigned locally the
    vale of xoxo.menu or xoxo/menu (same thing),
    and happy would be assigned the value of
    xoxo.sad. The neat yhimg about this is the
    vector could be constructed dynamically.

- parse ObjectMap { menu, sad: happy }
  - ObjectMap extends List
  - new ObjectMap(§menu, List.make(§sad, §happy))
- Rename List to Bubbles
- bubblescript files should not pollute the
  rootBinding/global namespace. They should
  instead get their own binding and be allowed
  to export a module object. Would like to add
  some object syntax. Also, thinking about a
  const function that works similiar to js
  const, bassically define/muf with a check.
- const { map } (require "list");
  - This would extract the map function into
    "map" symbol locally.
- const list (require "list");
- list/map °(1 2 3);

- better error handeling.
  - Missing closing parens or braces should give line number and colimn with helpful error message
  - better error for when getting such and such is not a function. continue improving
- bubl native each
- bubl native map
- bubls native list functions
- logging configu
- exception log
- repl log
- ensure townhouses handheld teams.
- ensure tokenizer handels tabs.
- Token type for list delimiteir, open close.
- token type for vector delimiter, open, close.
- single line comment token type )consumed with comment by tokenizer, or emmited depending on tokenizer configuration config.
- comma token type )emitted or consumed by tokenizer depending upon configuration.
- continue & implementation, needs more work, exteranl and internal functions.

- comma token type
- semicolon token type
- handl ctrl-z for repl
- comma at end of line, continues bare list, otherwise the are ignored, but for the sake of bare list continuation they must be passed from the tokenizer and handeled/considered by the parser.
- semi colon terminates bare list
- each and map for core
- Iterator for eval, integrate new parser and tokenizer.
- configuration for bare list parsing, with or without. Tokenizer can ignore newline tokens for this
- Allow esacping new lines for continuing bare list
- bare list (list without outer parens)
- parser, improved
- tokenizer, improved
- let tokenize and parse use list
- fix parse to move top down, or beginning to end.
- {} object parseing
- events for loading and configuration
- Allow raw list at top level in scripts, terminated with \n, continued with \\\n (\<newline>)
- Better error handling
  - Need to keep track of track line numbers through parsing. Annotations?
  - Error handling, and printing staxk traces form List.eval.
  - Interleaved stack traces js -> bubls -> js -> ...
- write test for applyArguments
- Implement index and ranges and use index to
  improve split, versus using find and until.
- Handel & for function parmeter for definition
  and calling.
- New define/muf that handels scheme style
  function definitions
  - Add tests
- Macro expansion is a bit of a hack, using
  throw, but it seems to be only way to do it
  without compilation step for the time being.
  Need to make sure it is handeled at every
  layer of evaluation. right now mapEval doesnt
  handel it, so this probably creates a gnarly
  bug if macro are used in parameter or within
  anpther macro, as it would pop all the way up
  to eval. This should be handeled, but also
  with mindfulness to not letting the code get
  unrurally with duplication and unnessecary
  complexity. something to think about and
  balance without priorites. Been thinking about
  a compilation step, which could be useful and
  this may tir back into this, however, also
  what to keep moving and playing with the
  language now that a good part of the base
  features seem to be present and working.
  Research and experimention into functional
  design theory and application.
July
- Package bubblescript to run in browser
  - bubblescript for web dev
  - binding to events
- Better docs, (Literate code style,
  literate code documentation generator.
- & to append list with params on
  function call. Use with puts.
- ensure implementation for & in function
  params.

- namespaces
- Update to use new es6 import
- document/integrate multiline repl
- lazy evaluation, research proof of
  concept
- car cdr cadr cdar caadr cddar...
- threading macro, ->

x ensure Tokenize and parse &
x throw MacroExpanded jit
x Impliment macros and make sure the
  expand when building the parse tree or
  just in time.
x invert seems to be converting list to
  vector, is bug?
x Clean up the Readme
x add MiT license
x Make macros work
x pop, push in bubblescript
x better pretty printing objects in repl
x Need to use empty lists and empty
  bubbles to make them object oriented
  again and get the best of both worlds by
  using a funtion which sends the message
  to the object, i.e. push, set
  root_binding

June
- -v --version for command bubls
- Make Repl Function
  - Options for repl

May
- Automatic invokation
- Parinfer for repl
- toString for macro
  - As a user, i should be be able to get the
    string representation of a macro by
    sending the toString message to a macro
    object.
