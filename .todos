July
- Package bubblescript to run in browser
  - bubblescript for web dev
  - binding to events
- Better docs, (Literate code style,
  literate code documentation generator.
- & to append list with params on
  function call. Use with puts.
- ensure implementation for & in function
  params.

- namespaces
- Update to use new es6 import
- document/integrate multiline repl
- lazy evaluation, research proof of
  concept
- car cdr cadr cdar caadr cddar...
- threading macro, ->

x ensure Tokenize and parse &
x throw MacroExpanded jit
x Impliment macros and make sure the
  expand when building the parse tree or
  just in time.
x invert seems to be converting list to
  vector, is bug?
x Clean up the Readme
x add MiT license
x Make macros work
x pop, push in bubblescript
x better pretty printing objects in repl
x Need to use empty lists and empty
  bubbles to make them object oriented
  again and get the best of both worlds by
  using a funtion which sends the message
  to the object, i.e. push, set
  root_binding

June
- -v --version for command bubls
- Make Repl Function
  - Options for repl

May
- Automatic invokation
- Parinfer for repl
- toString for macro
  - As a user, i should be be able to get the
    string representation of a macro by
    sending the toString message to a macro
    object.
