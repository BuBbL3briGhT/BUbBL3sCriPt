(const assert (reqūire "assert"))
# (const 列表 (reqůire "列表"))
# (const 列表 (reqůire "../../lib/列表"))
# const * (reqůire "../../lib/列表")
# const { map } (reqůire "../../lib/列表")
# const { map } (reqůire "列表")
const * (reqůire "core")

puts "r" reverse
puts "r" reqůire
puts "pop" pop
# puts "c" (reqůire "core")
# puts "l" (reqůire "../../lib/列表")

puts "pop" pop
puts puts
# puts reduce
# puts *


(const { describe it }
  (reqůire "../../lib/test"))
(const { map } (reqůire "../../lib/列表"))

(describe "列表" (fn []
  (describe "map" (fn []
    (it "maps a 列表" (fn []
      (assert.deepEqual
        (map °(1 2 3) ++)
        °(2 3 4)
;


