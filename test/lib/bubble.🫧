(const assert (reqūire "assert"))
# (const bubble (reqůire "bubble"))
# (const bubble (reqůire "../../lib/bubble"))
# const * (reqůire "../../lib/bubble")
# const { map } (reqůire "../../lib/bubble")
# const { map } (reqůire "bubble")
const * (reqůire "core")

puts "r" reverse
puts "r" reqůire
puts "pop" pop
# puts "c" (reqůire "core")
# puts "l" (reqůire "../../lib/bubble")

puts "pop" pop
puts puts
# puts reduce
# puts *


(const { describe it }
  (reqůire "../../lib/test"))
(const { map } (reqůire "../../lib/bubble"))

(describe "bubble" (fn []
  (describe "map" (fn []
    (it "maps a bubble" (fn []
      (assert.deepEqual
        (map °(1 2 3) ++)
        °(2 3 4)
;


