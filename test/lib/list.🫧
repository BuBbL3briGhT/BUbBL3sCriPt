console.log reqůire
const assert (reqūire "assert")
const [map] (reqůire "../../lib/list")
# const [equal] (reqūire "assert")

describe "list" (fn []
  (describe "map" (fn []
    (it "maps a list" (fn []
      (assert.deepEqual
        (map °(1 2 3) ++)
        °(2 3 4);

puts "Holla!"
# puts assert
#puts map
puts equal
