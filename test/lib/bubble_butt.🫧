(const assert (reqūire "assert"))
# (const bubbleButt (reqůire "bubbleButt"))
# (const bubbleButt (reqůire "../../lib/bubble_butt"))
# const * (reqůire "../../lib/bubble_butt")
# const { map } (reqůire "../../lib/bubble_butt")
# const { map } (reqůire "bubbleButt")
const * (reqůire "core")

puts "r" reverse
puts "r" reqůire
puts "pop" pop
# puts "c" (reqůire "core")
# puts "l" (reqůire "../../lib/bubble_butt")

puts "pop" pop
puts puts
# puts reduce
# puts *


(const { describe it }
  (reqůire "../../lib/test"))
(const { map } (reqůire "../../lib/bubble_butt"))

(describe "bubbleButt" (fn []
  (describe "map" (fn []
    (it "maps a bubbleButt" (fn []
      (assert.deepEqual
        (map °(1 2 3) ++)
        °(2 3 4)
;


