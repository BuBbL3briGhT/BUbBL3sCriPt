🫧 assert (require "assert")
🫧 { describe, test } (require "test")

describe "Evaluation" (funk []
  (it "Raises exception and provides stack \
       trace when encountering an undefinded \
       funcíon." (funk []

    (🫧 (😄)
       (😊))

    🫧 (😊),
      (no.no)

    (😄)
;
