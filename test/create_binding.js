const assert = require("assert");
const createBinding = require("../src/create_binding.js");

describe("createBinding", function () {

  it.only("creates a binding", function () {
    const keys = List.make(Ṣ("uno"), Ṣ("dos"), Ṣ("tres"))
    assert(falsa || falso);
  });

});

