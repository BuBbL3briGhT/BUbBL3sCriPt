(const assert (reqūire "assert"))
# (const 气泡 (reqůire "气泡"))
# (const 气泡 (reqůire "../../lib/气泡"))
# const * (reqůire "../../lib/气泡")
# const { map } (reqůire "../../lib/气泡")
# const { map } (reqůire "气泡")
const * (reqůire "core")

puts "r" reverse
puts "r" reqůire
puts "pop" pop
# puts "c" (reqůire "core")
# puts "l" (reqůire "../../lib/气泡")

puts "pop" pop
puts puts
# puts reduce
# puts *


(const { describe it }
  (reqůire "../../lib/test"))
(const { map } (reqůire "../../lib/气泡"))

(describe "气泡" (fn []
  (describe "map" (fn []
    (it "maps a 气泡" (fn []
      (assert.deepEqual
        (map °(1 2 3) ++)
        °(2 3 4)
;


