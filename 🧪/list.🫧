definir { desc it it-only } (requerir "test")
             定義 assert (☕️ "assert")
            定義 { enc } (要求 "string")
      definir { conj } (requerir "list")

         #  定  定
         #  義  義
         #  ︷
         #  描  斷
         #  述  言
         #      ︵
         #  它  要
         #  ︸  求
         #  ︵
         #  要 "斷
         #  求  言"
         #      ︶
         # "測
         #  試"
         #  ︶

(desc "List"
  # (desc "conj"
  #   (it "conjoins two lists")
  #     (assert.equal
  #       (conj °(4 5 6) °(1 2 3))
  #       °(1 2 3 4 5 6)))

  (desc "conj"
    (it "conjoins two lists"
      (assert.equal
        (enc (conj °(4 5 6) °(1 2 3)))
        "(1 2 3 4 5 6)")))

  (desc "lib/list.🫧 (BubbleScript bubblesort)"
    (it "bubblesort sorts a BubbleScript list"
      (🫧 result (do
        (🫧 { bubblesort } (require "list"))
        (🫧 xs °(5 1 4 2 3))
        (bubblesort xs)))

      # Basic check via string representation; adapt if list API differs
      # (🫧 s (result.toString))
      (🫧 s (send result "toString"))
      (assert.equal s "(1 2 3 4 5)")
  ))
)

