
: { desc it } ("test")
: { array } ("array")
: { encodar } ("string")
: assert ("assert")

# desc "array"
#   it "creates an array"
#     : a (array 1 2 3)
#     assert.equal (encodar a) "1,2,3"

(desc "array"
  (it "creates an array"
    (: a (array 1 2 3))
    (assert.equal (encodar a) "1,2,3")))



