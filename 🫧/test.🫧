(🫧 Mocha (require-☕️ "mocha"))
(🫧 { push } (require "list"))
: assert ("assert")

(define-macro (desc title & body)
  (list Mocha.describe title
    (push body °(jsfn))))

(define-macro (it title & body)
  (list Mocha.it title
    (if body (push body °(jsfn)))))

(define-macro (it-only title & body)
  (list Mocha.it.only title
    (if body (push body °(jsfn)))))

(module.exports {desc it it-only assert})

