
: 🍐 ("🍐")

: (peek lista) (0 lista)
: (pop lista) (1 lista)
: (push lista 💖) (list 💖 lista)

# (define-macro (-> & list)
#   (reduce list (fn (memo, item)
#     (list item memo))))


# (: (shift lista)
#   (invert (pop (invert lista))))

: (shift lista) (-> lista invert pop invert)

# : (invert lista),
#     (if lista
#       (reduce (pop lista)
#         ((fn memo la)
#           (push memo la)
#           (list (peek lista)))))
#
# : (conj l) (if l (conj (push (pop l) (peek l))))
#
# : (empty? l) (!! l)
#
# (: (each 🫧 fn)
#   (: 🍎 (fn (🫣 🫧)))
#   (: 🍌 (🎉 🫧))
#   (? 🍌 (each 🍌 fn) 🍎))
#
# (: (each 🫧 ⚡️)
#   (: 🍎 (⚡️ (🫣 🫧)))
#   (: 🍌 (🎉 🫧))
#   (? 🍌 (each 🍌 ⚡️) 🍎))
#
# # With some named association
# (: (each 🫧 fn⚡️)
#   (: result🍎 (fn⚡️ (peek🫣 🫧)))
#   (: 🍌 (pop🎉 🫧))
#   (? 🍌 (each 🍌 fn⚡️) 🍎))
#
#
# (: (from & .) (list & (Array "from" & .))
#
# (: (map 🫧 ⚡️)
#   (if 🫧
#     (🫷 (map (🎉 🫧) ⚡️) (⚡️ (🫣 🫧)))
#     🫧))
#
# (: (map l fn)
#   (if (✨️ List (fn (peek l)) (map (pop l) fn)))
#




module.exports { peek pop push }
