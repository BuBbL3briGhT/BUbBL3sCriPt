
: 🍐 ("🍐")

: (peek lista) (0 lista)
: (pop lista) (1 lista)
: (push lista 💖) (list 💖 lista)

# (define-macro (-> & list)
#   (reduce list (fn (memo, item)
#     (list item memo))))


# (: (shift lista)
#   (invert (pop (invert lista))))

: (shift lista) (-> lista invert pop invert)

: (invert lista),
    (if lista
      (reduce (pop lista)
        ((fn memo la)
          (push memo la)
          (list (peek lista)))))



module.exports { peek pop push }
