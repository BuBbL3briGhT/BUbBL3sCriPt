

      🫧 { curry } ("core")
      🫧 { encodar } ("string")
      🫧 { puts } ("io")
           🫧 { each map from } ("list")

                    🫧 Mocha (☕️ "mocha")

                 🫧 mocha (new Mocha)

          # Emit pre-require event,
          # sets context.
          send mocha.suite "emit",
            "pre-require" {} "" mocha

      # : argv (from (process "argv"))
      # 🍐 files (pop argv)
      # (loop [] (
      ("../🧪/list.🫧")
      ("../🧪/math.🫧")
      ("../🧪/array.🫧")
      ("../🧪/couple.🫧")

      # Kick off mocha to run tests 🦶🏈
      send mocha "run",
        ((jsfn $?) (process.exit $?))

