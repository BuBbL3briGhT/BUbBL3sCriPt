

Right now chasing down issues with getting a decent
stack traces printing for errors.

This isn't so much a big, s it is a fair implementation that we are just having issues worth how it needs to be designed. i think we tried to take a short cut but speeding the attack gave into the binding and that seems to be the primary source of the confusion. it sends the best place to record the stack calls is at the eval funcrion for list., how've there was an strange thing happening which now seems to be resolve to the fact that the function has is own binding object, so when we invoke a function it has a different binding then the binding object which is passed to the call function of the fnobject. That binding is only used to resolve the parameters, so throw is an issue if we are trying to use the binding to collect the stack layer. Also we werw having an ussue even earlier which was resolved to the fact we were trying to collect the stack in the evalExpression method, but the is a call inside of the list eval to resolve the head, which uses the binding so that xreated an extena anomolus stack call.

Anyway, need to give it so thouggt as to the correct way to track the stack. i was trying to avoid passing it aroynd as an extra parameter but thinking about it that is probably the inly cprrect way, other than have an evaloator object or something, which would probably serve basically the same purpose, or having a global value, but that would almost certianly limit execuation to a si gle treaf context, no potential for parralelusn anything with out redesign the implementation of the language.

i think at this point there is enough evidenece to confim we need to pass the stack as an extra parameter, suffice voodoo black magic to some how tuck it away in another cpntext which almost certian to bring design trade offs and and the potential for bugs for no good reason, simply obsxuraty and needlessly handycaping the code which should be exposed to it if it is a concern. it's true that the stack is implied, as the calls for the functions are reflected already in the program memory srructure, only we can't get access to it without popping it, this isfair for proformace, however that isnt an impRative, perhaps with some reflection feature in js, there could be away of achoeving it, or someother work around, i think for now, the most flexable appraoch will bw keeping a stack parameter explict that we pass arpund where it is needed, and the can be removed if it uzrfulness ever no longer needed, or interms of an optimized execution or something, i think simply it is where we need to start and to worl from.

Apologizes that a lot if these commits have simply
been commentless woeking commits, bit just been
working thrpugh this and we some different
distractions, ans now the code has sorta divereged
between multi vetbal languages, not neccesarily,
neccesary, but anyway, just grinding on this to try
to vet a suffecent support and direction for these la
gague feature which we should have, as otherqise
there is not a lot of claroty in how bubblescript is
expected to operate. it mostly for convienxe, but i
think it is expected and neccesary for the language
practicality. Current focused primary on errors
resulting calling an undefined fun tion, umdpubtedly
there are error types which we will need to add
support for, but starting with one, and going from
there will give us the best understanding of what we
are truly dealing with so that we can arrove
hopefully at the best solution.

---

After some thought,  im pretty sure the simplest way
to handel it is probably entirley from the List#eval
method, i think i may be able to be entirely handel
there in the exception handel with a catch statement,
and just being mind full to offset and collect the
containing function name before printing the line.
Really we are just summarizing the larger
bubblescript stach trace kind of, with some
annoatation from collected internal values, but not
just summarizing, but brunging into focus and showing
the relevent aspects of the stack trace for the
client code.


If we wanted to capture the stack trace it code be
possible to some how take a memory shot of tge
application, then unwind the stack with an exception,
then return to the application saved point, but i
wouldn't know if that is possible, or how to do that
in js, however in theory it makes sense, just
question is how to do it, and if the suppott is
there, tracking the pila/stack provides the most
flexibility, and is straight forward and simple
enough, simplifies the code greatly compared with the
faulty binding variable attempt, eitherway, it is an
extra diaganostic aspect/concern which can be skipped
for profarmance or purity reasons if we are to rely
on catching up the stack to construct the trace only
on errors, which is fine for the primary need, and
like i said, can probably summed up soley in
List#eval, although the logic would be spread out
over each step up, and would need to work together to
get the right output. That's the just of it. I can
just do it, but i wanted to talk about just so the
context of the goals and desires can be highlighted,
as itmay providesome helpfuldesign insight that might
otherwise get over looked if we are just work in or
translating directly into code without any extra
treatment or discussion.


---

Also, just of quick note, the current stack trace, which looks like this:

bubblescript 🍀 bin/bubls miaplicación.🫧
Hola
{ createBindingiProto: undefined }
{ createBinding: undefined }
{ createBindingiProto: undefined }
{ createBinding: undefined }
{ pila: '([object Object] [object Object])' }
Error: La funcíon "no.no" no está definida.
    en 😊 (miaplicación.🫧:12:4)
    en 😄 (miaplicación.🫧:17:1)


Isn't showing the line number of the error, and as i
alluded to earlier, the en function are off, this is
how the i formation is collected, but for display, we
need to offset that the funxtion we are in, which is
the one that was called is with the line from the
function, npt the line it was called on. I think that
is consistant with the Node stack trace, and i think
makes the most since, no matter what, the way it is
presented would at least need to change, because it
is saying the the call to the function in in
thefunctuon right now, amd that in not correct,
however this is an incedental detail because this is
just raw outputfrom iteration, and we've been
fovusing on other design details, but just making tge
note here for claroty of ultimate intentions.

---

As hopefully a final note, we started collecting the
stack from within the Fn#call method, which was a
fine place to start from as it was a guess, still
guessing, but as mentioned, it seems List#eval is the
right place to do this from, and is currently where
pila is incepting from. Think it through,Fn#call
could potentaily work, but we were running into
issue, so it might not, i think List#eval is the
right place, well see. Also tracing pila as a
paramter was useful, just to see what all yhr
methods/function which are involved with the
evaluation call chain.
