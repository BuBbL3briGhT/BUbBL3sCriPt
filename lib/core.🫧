(define (push a b)
  (send a :push b))

(define (puts & msg)
  (console.log & msg))

(define (peek list)
  (send list :peek))

(define (pop list)
  (send list :pop))

(define (invert list)
  (send list :invert))

(define (reverse list)
  (send list :invert))

