(muf (push a b) (send a :push b)))

(muf (puts & msg)
  (console.log & msg))

(muf (peek list) (send list :peek)))
(muf (pop list) (send list :pop)))
(muf (invert list) (send list :invert)))
(muf (reverse list) (send list :invert)))

